parameters:
  - name: pool
    type: object
    default:
      vmImage: 'ubuntu-24.04'
  - name: container
    type: string
    default: ''

  # Checkout
  - name: checkoutFetchDepth
    type: number
    default: 0           # 0 = full history (needed for GitVersion)
  - name: checkoutFetchTags
    type: boolean
    default: true
  - name: checkoutLfs
    type: boolean
    default: true

  # Job wiring
  - name: dependsOn
    type: object
    default: []                     # optional: list of job names this job depends on
  - name: displayName
    type: string
    default: 'Security: GHAS + Trivy'
  - name: jobName
    type: string
    default: security_scan

  # Caller-supplied init steps (run after checkout)
  - name: initSteps
    type: stepList
    default: []

  # Caller-supplied finalization steps (run at end)
  - name: finalizationSteps
    type: stepList
    default: []

  # Switches
  - name: runCodeQL
    type: boolean
    default: true                   # enable/disable CodeQL
  - name: runDependencyCheck
    type: boolean
    default: true          # enable/disable GHAS dependency scanning
  - name: runLicenseScan
    type: boolean
    default: true

  # CodeQL
  - name: codeqlLanguages
    type: string
    default: 'csharp,javascript'

  # .NET inputs — either explicit list or globs
  - name: dotnetSolutionsOrProjects
    type: object
    default: []
  - name: dotnetProjectGlobs
    type: string
    default: ''
  - name: dotnetBuildConfiguration
    type: string
    default: 'Release'

  # NuGet feeds
  - name: feedsToUse
    type: string
    default: 'config'              # 'config' | 'select' | 'feed'
  - name: nugetConfigPath
    type: string
    default: 'nuget.config'
  - name: externalFeedCredentials
    type: string
    default: ''

  # Extra args
  - name: dotnetRestoreExtraArgs
    type: string
    default: ''
  - name: dotnetBuildAdditionalArgs
    type: string
    default: ''
  - name: npmFolders
    type: object
    default: []

  # Containers
  - name: imagesToScan
    type: object
    default: []
  - name: dockerfilesToBuild
    type: object
    default: []
  - name: dockerBuildContext
    type: string
    default: '.'
  - name: dockerBuildArgs
    type: string
    default: ''
  - name: containerRegistryServiceConnection
    type: string
    default: ''
  # Helm
  - name: helmChartFolders
    type: object
    default: []

  # Trivy tuning
  - name: trivyVersion
    type: string
    default: '0.73.0'
  - name: trivyCacheDir
    type: string
    default: '$(Pipeline.Workspace)/.trivycache'
  - name: trivySeverityFail
    type: string
    default: 'CRITICAL,HIGH'
  - name: trivySeverities
    type: string
    default: 'UNKNOWN,LOW,MEDIUM,HIGH,CRITICAL'
  - name: trivyFailOnSeverityThreshold
    type: string
    default: 'HIGH'
  - name: trivyIgnoreUnfixed
    type: boolean
    default: true
  - name: trivyExitCodeVuln
    type: number
    default: 1
  - name: trivyFsTargets
    type: object
    default: ['.']
  - name: trivyFsScanners
    type: string
    default: 'vuln,secret' # license scanner is in a separate task
  - name: trivyFsSkipDirs
    type: object
    default: []
  - name: trivyFsSkipFiles
    type: object
    default: []
  - name: trivyConfigPath
    type: string
    default: ''

  # SARIF
  - name: publishSarif
    type: boolean
    default: true

jobs:
- job: ${{ parameters.jobName }}
  displayName: ${{ parameters.displayName }}

  # Optional dependsOn
  ${{ if gt(length(parameters.dependsOn), 0) }}:
    dependsOn: ${{ parameters.dependsOn }}

  pool: ${{ parameters.pool }}

  ${{ if ne(parameters.container, '') }}:
    container: ${{ parameters.container }}

  steps:
    - checkout: self
      persistCredentials: true
      fetchDepth: ${{ parameters.checkoutFetchDepth }}
      fetchTags: ${{ parameters.checkoutFetchTags }}
      lfs: ${{ parameters.checkoutLfs }}

    - ${{ if eq(parameters.container, '') }}:
      - task: UseDotNet@2
        displayName: 'Install .NET SDK (global.json)'
        inputs:
          packageType: sdk
          useGlobalJson: true

    - script: |
        echo "dotnet: $(dotnet --version)"
        node --version || true
        npm --version  || true
      displayName: 'Environment info'

    - bash: mkdir -p "$(Build.SourcesDirectory)/reports"
      displayName: 'Ensure reports directory'

    # Caller init steps
    - ${{ each _step in parameters.initSteps }}:
      - ${{ _step }}

    # 0) Optional: registry login
    - ${{ if ne(parameters.containerRegistryServiceConnection, '') }}:
      - task: Docker@2
        displayName: 'Docker: login'
        inputs:
          command: login
          containerRegistry: ${{ parameters.containerRegistryServiceConnection }}

    # 1) CodeQL INIT (only if CodeQL is enabled)
    - ${{ if eq(parameters.runCodeQL, true) }}:
      - task: AdvancedSecurity-Codeql-Init@1
        displayName: 'GHAS: CodeQL init'
        inputs:
          languages: ${{ parameters.codeqlLanguages }}

    # 2) Restore (needed if either CodeQL OR Dependency Scanning is enabled)
    - ${{ if or(eq(parameters.runDependencyCheck, true), eq(parameters.runCodeQL, true)) }}:
      - ${{ if ne(parameters.dotnetProjectGlobs, '') }}:
        - task: DotNetCoreCLI@2
          displayName: 'dotnet restore (globs, lockfiles)'
          inputs:
            command: 'restore'
            projects: ${{ parameters.dotnetProjectGlobs }}
            feedsToUse: ${{ parameters.feedsToUse }}
            ${{ if eq(parameters.feedsToUse, 'config') }}:
              nugetConfigPath: ${{ parameters.nugetConfigPath }}
            ${{ if or(eq(parameters.feedsToUse, 'feed'), eq(parameters.feedsToUse, 'select')) }}:
              externalFeedCredentials: ${{ parameters.externalFeedCredentials }}
            arguments: '--locked-mode ${{ parameters.dotnetRestoreExtraArgs }}'

      - ${{ if eq(parameters.dotnetProjectGlobs, '') }}:
        - ${{ each path in parameters.dotnetSolutionsOrProjects }}:
          - script: |
              echo "Restoring (lockfile) ${{ path }}"
              dotnet restore "${{ path }}" --locked-mode ${{ parameters.dotnetRestoreExtraArgs }}
            displayName: 'dotnet restore (lockfile): ${{ path }}'

    # 3) GHAS Dependency Scanning (only if enabled)
    - ${{ if eq(parameters.runDependencyCheck, true) }}:
      - task: AdvancedSecurity-Dependency-Scanning@1
        displayName: 'GHAS: Dependency scanning (NuGet & npm)'

    # 4) Manual build for CodeQL (only if CodeQL is enabled)
    - ${{ if eq(parameters.runCodeQL, true) }}:
      - ${{ if ne(parameters.dotnetProjectGlobs, '') }}:
        - task: DotNetCoreCLI@2
          displayName: 'dotnet build (globs for CodeQL)'
          inputs:
            command: 'build'
            projects: ${{ parameters.dotnetProjectGlobs }}
            arguments: '-c ${{ parameters.dotnetBuildConfiguration }} --no-restore ${{ parameters.dotnetBuildAdditionalArgs }}'
      - ${{ if eq(parameters.dotnetProjectGlobs, '') }}:
        - ${{ each path in parameters.dotnetSolutionsOrProjects }}:
          - script: |
              echo "Building ${{ path }}"
              dotnet build "${{ path }}" --configuration ${{ parameters.dotnetBuildConfiguration }} --no-restore ${{ parameters.dotnetBuildAdditionalArgs }}
            displayName: 'dotnet build: ${{ path }}'

    # 5) CodeQL analyze (only if CodeQL is enabled)
    - ${{ if eq(parameters.runCodeQL, true) }}:
      - task: AdvancedSecurity-Codeql-Analyze@1
        displayName: 'GHAS: CodeQL analyze'

    # 6) Trivy scans

    # trivy@2 accepts additional CLI flags through `options`. Several v1 input
    # names (for example severity, format, output and trivyCacheDir) are ignored
    # by v2, so build the common options explicitly once for every scan.
    - bash: |
        set -euo pipefail

        FASTFOOD_TRIVY_OPTIONS="--cache-dir ${FASTFOOD_TRIVY_CACHE_DIR}"
        if [ -n "${FASTFOOD_TRIVY_CONFIG_PATH}" ]; then
          FASTFOOD_TRIVY_OPTIONS="${FASTFOOD_TRIVY_OPTIONS} --config ${FASTFOOD_TRIVY_CONFIG_PATH}"
        fi
        if [ -n "${FASTFOOD_TRIVY_SKIP_DIRS}" ]; then
          FASTFOOD_TRIVY_OPTIONS="${FASTFOOD_TRIVY_OPTIONS} --skip-dirs ${FASTFOOD_TRIVY_SKIP_DIRS}"
        fi
        if [ -n "${FASTFOOD_TRIVY_SKIP_FILES}" ]; then
          FASTFOOD_TRIVY_OPTIONS="${FASTFOOD_TRIVY_OPTIONS} --skip-files ${FASTFOOD_TRIVY_SKIP_FILES}"
        fi

        echo "##vso[task.setvariable variable=FastFoodTrivyOptions]${FASTFOOD_TRIVY_OPTIONS}"
      displayName: 'Configure Trivy v2 options'
      env:
        FASTFOOD_TRIVY_CACHE_DIR: ${{ parameters.trivyCacheDir }}
        FASTFOOD_TRIVY_CONFIG_PATH: ${{ parameters.trivyConfigPath }}
        FASTFOOD_TRIVY_SKIP_DIRS: ${{ join(',', parameters.trivyFsSkipDirs) }}
        FASTFOOD_TRIVY_SKIP_FILES: ${{ join(',', parameters.trivyFsSkipFiles) }}

    # Report all severities, but fail only on fixable findings at or above the
    # configured threshold. Scanner/runtime errors always remain blocking.
    - ${{ each tgt in parameters.trivyFsTargets }}:
      - task: trivy@2
        displayName: 'Trivy FS scan: ${{ tgt }} (${{ parameters.trivyFsScanners }})'
        inputs:
          type: filesystem
          target: '${{ tgt }}'
          scanners: '${{ parameters.trivyFsScanners }}'
          severities: '${{ parameters.trivySeverities }}'
          ignoreUnfixed: ${{ parameters.trivyIgnoreUnfixed }}
          failOnSeverityThreshold: '${{ parameters.trivyFailOnSeverityThreshold }}'
          ignoreScanErrors: false
          reports: sarif
          options: '$(FastFoodTrivyOptions)'
          version: ${{ parameters.trivyVersion }}
        continueOnError: false

    - ${{ if eq(parameters.runLicenseScan, true) }}:
      - ${{ each tgt in parameters.trivyFsTargets }}:
        - task: trivy@2
          displayName: 'Trivy FS scan: ${{ tgt }} (license)'
          inputs:
            type: filesystem
            target: '${{ tgt }}'
            scanners: 'license'
            severities: '${{ parameters.trivySeverities }}'
            failOnSeverityThreshold: '${{ parameters.trivyFailOnSeverityThreshold }}'
            ignoreScanErrors: false
            reports: sarif
            options: '$(FastFoodTrivyOptions)'
            version: ${{ parameters.trivyVersion }}
          continueOnError: false

    # Build & scan each Dockerfile (CLI)
    - ${{ each df in parameters.dockerfilesToBuild }}:
      - bash: |
          set -euo pipefail
          mkdir -p "$(Build.SourcesDirectory)/reports"
          SAFE_NAME=$(basename "${{ df }}" | tr '[:upper:]' '[:lower:]' | sed 's/[^a-z0-9]/-/g')
          TAG="${SAFE_NAME}:$(Build.BuildId)"
          echo "Building ${{ df }} → ${TAG}"
          docker build -f "${{ df }}" ${{ parameters.dockerBuildArgs }} -t "${TAG}" "${{ parameters.dockerBuildContext }}"

          echo "Scanning image ${TAG} with Trivy (CLI)"
          FASTFOOD_CONFIG_ARGS=()
          if [ -n "${{ parameters.trivyConfigPath }}" ]; then
            FASTFOOD_CONFIG_ARGS=(--config "${{ parameters.trivyConfigPath }}")
          fi

          trivy --version
          trivy image \
            --cache-dir "${{ parameters.trivyCacheDir }}" \
            --format sarif \
            --output "$(Build.SourcesDirectory)/reports/trivy-image-${SAFE_NAME}.sarif" \
            --ignore-unfixed \
            --pkg-types "os,library" \
            --severity "${{ parameters.trivySeverityFail }}" \
            --exit-code "${{ parameters.trivyExitCodeVuln }}" \
            "${FASTFOOD_CONFIG_ARGS[@]}" \
            "${TAG}" || EXIT=$?

          if [ "${EXIT:-0}" -ne 0 ]; then
            echo "Trivy found policy violations."
            exit "${EXIT}"
          fi
          echo "Trivy scan passed."
        displayName: 'Docker build+scan: ${{ df }}'

    # Scan any pre-built images (task-based)
    - ${{ each image in parameters.imagesToScan }}:
      - task: trivy@2
        displayName: 'Trivy image scan: ${{ image }}'
        inputs:
          type: image
          target: '${{ image }}'
          scanners: vuln
          severities: '${{ parameters.trivySeverities }}'
          ignoreUnfixed: ${{ parameters.trivyIgnoreUnfixed }}
          failOnSeverityThreshold: '${{ parameters.trivyFailOnSeverityThreshold }}'
          ignoreScanErrors: false
          reports: sarif
          options: '$(FastFoodTrivyOptions) --pkg-types os,library'
          version: ${{ parameters.trivyVersion }}
        continueOnError: false

    # Helm charts (misconfig) → SARIF
    - ${{ each chart in parameters.helmChartFolders }}:
      - task: trivy@2
        displayName: 'Trivy config scan (Helm): ${{ chart }}'
        inputs:
          type: filesystem
          target: '${{ chart }}'
          scanners: 'misconfig'
          severities: '${{ parameters.trivySeverities }}'
          failOnSeverityThreshold: '${{ parameters.trivyFailOnSeverityThreshold }}'
          ignoreScanErrors: false
          reports: sarif
          options: '$(FastFoodTrivyOptions)'
          version: ${{ parameters.trivyVersion }}
        continueOnError: false

    # trivy@2 writes its JSON and generated SARIF reports below the agent temp
    # directory. Gather them so GHAS and the raw-report artifact keep working.
    - bash: |
        set -euo pipefail
        FASTFOOD_TRIVY_TEMP="$(Agent.TempDirectory)/trivy"
        FASTFOOD_REPORTS="$(Build.SourcesDirectory)/reports"
        mkdir -p "${FASTFOOD_REPORTS}"

        if [ ! -d "${FASTFOOD_TRIVY_TEMP}" ]; then
          echo "No Trivy report directory was created."
          exit 0
        fi

        find "${FASTFOOD_TRIVY_TEMP}" -maxdepth 1 -type f -name 'trivy-results-*.json' -exec cp {} "${FASTFOOD_REPORTS}/" \;
        while IFS= read -r FASTFOOD_SARIF; do
          FASTFOOD_SARIF_NAME=$(basename "${FASTFOOD_SARIF}" .json)
          cp "${FASTFOOD_SARIF}" "${FASTFOOD_REPORTS}/${FASTFOOD_SARIF_NAME}"
        done < <(find "${FASTFOOD_TRIVY_TEMP}" -maxdepth 1 -type f -name 'trivy-results-*.sarif.json')
      displayName: 'Collect Trivy v2 reports'
      condition: succeededOrFailed()

    # 7) Publish Trivy SARIF
    - ${{ if eq(parameters.publishSarif, true) }}:
      - task: AdvancedSecurity-Publish@1
        displayName: 'Publish SARIF (Trivy) to Advanced Security'
        condition: succeededOrFailed()
        inputs:
          SarifsInputDirectory: '$(Build.SourcesDirectory)/reports'
          WaitForProcessing: true

    - task: PublishPipelineArtifact@1
      displayName: 'Publish raw security reports'
      condition: succeededOrFailed()
      inputs:
        targetPath: '$(Build.SourcesDirectory)/reports'
        artifact: 'security-reports'

    # Caller finalization steps
    - ${{ each _step in parameters.finalizationSteps }}:
      - ${{ _step }}
