# Agent guidance

The canonical repository guidance is `.github/copilot-instructions.md` with
path-scoped rules in `.github/instructions/`. Read those files before changing code.

Reusable workflows are canonical under `.github/skills/`. Codex discovers the same
skills through `.agents/skills`.
